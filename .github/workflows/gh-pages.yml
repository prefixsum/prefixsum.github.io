name: Build and deploy
on:
  push:
    branches:
      - "main"
      - "feat/new-site-2024-10"
permissions:
  contents: write
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4.2.1
        with:
          persist-credentials: false

      - name: Build 🔧
        run: |
          cd frontend
          yarn
          yarn generate

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@v4.6.8
        with:
          folder: frontend/dist
